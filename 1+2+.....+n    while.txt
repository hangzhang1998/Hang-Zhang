#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main (void)
{
    int i,n,sum;
    printf("Input n:\n");
    scanf("%d",&n);

    sum = 0;
    i = 1;

    while (i <= n)
    {
        sum = sum +i;
        i++;
    }
    printf("sum = %d\n",sum);
    return 0;
}
